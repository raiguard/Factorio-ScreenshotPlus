-- ----------------------------------------------------------------------------------------------------
-- PREVIEW GUI
-- A preview window that shows what the screenshot will look like.

local event = require('scripts/lib/event-handler')
local titlebar = require('scripts/gui/gui-elems/titlebar')
local mod_gui = require('mod-gui')
local util = require('scripts/lib/util')

local preview_gui = {}

-- --------------------------------------------------
-- LOCAL UTILITIES



-- --------------------------------------------------
-- EVENT HANDLERS



-- --------------------------------------------------
-- LIBRARY

function preview_gui.create(parent, player_index, settings)
    
end

return preview_gui